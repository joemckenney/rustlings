// functions1.rs
// Execute `rustlings hint functions1` or use the `hint` watch subcommand for a hint.

fn call_me(n: i32) {
    println!("called w/ {}", n)
}

fn main() {
    let number = 3;
    call_me(number);
}
